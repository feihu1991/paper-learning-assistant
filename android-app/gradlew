#!/bin/sh

# Gradle wrapper script (placeholder)
# In real project, this should be generated by Gradle

echo "Gradle wrapper - use ./gradlew commands"
echo "For GitHub Actions, the actual gradlew will be generated"
